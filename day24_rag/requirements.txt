# Day 14 需求：Chroma 只用「內嵌模式」（PersistentClient），不跑 server
openai>=1.40
numpy>=1.26
python-dotenv>=1.0
# 釘死版本：與 Day 11 同版，確保檢索側條件不變——今天要比的是生成端。
# 資安注意：CVE-2026-45829（未修補，CVSS 10.0）只影響把 Chroma 當
# Python server 對外服務的部署；本資料夾為內嵌模式、不開網路埠，不受影響。
chromadb==1.5.9
# Day 16 起用來離線重數 token（measure_cost.py、run_experiment_day17.py）：
# 快取全命中時 API 不回報 usage，成本表只能自己數
tiktoken>=0.7
# Day 22 起：把 markdown 排版成 PDF、render 成頁面圖（也用來抽 PDF 文字層）
PyMuPDF>=1.24
Pillow>=10.0
# Day 22 的 OCR：PaddleOCR 的 ONNX 版，離線跑、不打 API。
# 選它而不是視覺模型，是因為 gpt-4o／gpt-4o-mini 一律拒絕逐字轉錄整頁文件
# （20 次呼叫、3.44 元、一個字都沒拿到，證據在 vision_cache_day22.json）。
rapidocr-onnxruntime>=1.3

# Day 24 起：把腳本包成一支服務。
# handler 刻意寫成同步 def，Starlette 會丟進執行緒池——8 個請求就真的是
# 8 條執行緒同時跑，不然「並發下快取會壞」這件事根本量不到。
fastapi>=0.115
uvicorn>=0.30
# 實驗這一側用來打服務（thread-safe 的 Client，並發那幾輪共用一個）
httpx>=0.27
# Day 24 的服務測試：不打 API，全部走既有快取
pytest>=8.0
