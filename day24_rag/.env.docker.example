# 複製成 .env 再填。docker compose 會自動讀同目錄的 .env
OPENAI_API_KEY=sk-...

# 對外要開哪個 port
API_PORT=8024

# uvicorn worker 數。索引每個 worker 各建一份，記憶體要夠才往上調
DAY24_WORKERS=1

# Redis 記憶體上限，滿了會用 allkeys-lru 淘汰
REDIS_MAXMEMORY=512mb

# 要處理沒有文字層的掃描 PDF 才設 1（映像會多約 250 MB）
WITH_OCR=0
